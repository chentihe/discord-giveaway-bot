const multipleEntries = (member) => {
    // create a map to store the result of how many NFTs of users
    // ex: A user has 5 NFTs, then should store A: 5
};

const userEntries;

export default multipleEntries;